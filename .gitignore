__pycache__/
*.pyc
.env
data/
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# paper lives on arXiv, not in the repo
paper/
*.tar.gz
ARXIV_ABSTRACT.txt

# raw sweep outputs (regenerable)
sweep_*.json
data/sweeps/
data/logs/

# run logs
*.log

# human annotation: raw per-rater data kept private (annotators are anonymous);
# aggregate result lives in results/human_validation.txt
annotation/
ratings_blind.csv
ratings_blind_filled.csv
ratings_key.csv
ANNOTATE.md
